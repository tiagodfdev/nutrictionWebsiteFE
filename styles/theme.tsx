import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#204907',
    },
    secondary: {
      main: '#E8871E',
    },
  },
});

export default theme;
